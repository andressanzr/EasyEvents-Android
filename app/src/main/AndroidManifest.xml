<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="es.easyevents">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".Activity.ActivityGet"></activity>
        <activity android:name=".Activity.EventoDetail" />
        <activity android:name=".Activity.EventoPOST" />
        <activity android:name=".Activity.EventoBorrar" />
        <activity android:name=".Activity.EventoGETbuscar" />
        <activity android:name=".Activity.EventoPUT" />
        <activity android:name=".Activity.Privacidad" />
        <activity android:name=".Activity.PrincipalActivity" />
        <activity android:name=".Activity.FinaldelEvento" />
        <activity
            android:name=".Activity.ActivityMaps"
            android:parentActivityName=".Activity.EventoCreando" />
        <activity android:name=".Activity.Registrar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Activity.EventoCreando" />
        <activity android:name=".CreandoEvento" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.CrearEvento" />
        <activity android:name=".Activity.ModificarEvento" />
        <activity android:name=".Activity.Login" />
        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>